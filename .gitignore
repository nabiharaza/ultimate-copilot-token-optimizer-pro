# OS/editor metadata
.DS_Store
.idea/
.jbeval/
.vscode/

# Python environments, caches, and build metadata
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
*.egg-info/
build/
dist/

# Test coverage artifacts (pytest-cov leaves one of these per parallel worker)
.coverage
.coverage.*
htmlcov/

# Frontend dependencies and generated bundles
TrimP/dashboard/frontend/node_modules/
TrimP/dashboard/frontend/dist/

# Local runtime data and audit traces
data/*.db
data/*.db-*
data/*.log
data/archives/
*.log
*.pid
*.tmp
*.trace
.trimp/

# Stray DB created if a script is ever run from the repo root instead of
# writing to ~/.trimp/TrimP.db or data/TrimP.db (see migrate_all_data.py,
# import_to_correct_db.py) — real data always lives in one of those two spots.
/TrimP.db

# Unrelated nested workspaces kept locally for reference
/FluidVoice/
/token-optimizer/
